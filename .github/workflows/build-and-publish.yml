name: Build & Publish AppBundle to APS

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'bundle/**'
      - '.github/workflows/build-and-publish.yml'
  workflow_dispatch:   # allow manual trigger from GitHub UI

env:
  # Revit engine version to target
  ENGINE_VERSION: "2024"
  BUNDLE_NAME: "RevitExtractor"
  ALIAS: "prod"

jobs:
  build-and-publish:
    runs-on: windows-latest          # MUST be Windows — Revit SDK is Windows-only

    steps:
      # ── 1. Checkout ────────────────────────────────────────────────────────
      - name: Checkout repo
        uses: actions/checkout@v4

      # ── 2. Setup .NET ──────────────────────────────────────────────────────
      - name: Setup .NET Framework build tools
        uses: microsoft/setup-msbuild@v2

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v2

      # ── 3. Restore NuGet packages ──────────────────────────────────────────
      - name: Restore NuGet packages
        run: nuget restore src/RevitExtractor/RevitExtractor.csproj

      # ── 4. Build the plugin DLL ────────────────────────────────────────────
      - name: Build RevitExtractor.dll
        run: |
          msbuild src/RevitExtractor/RevitExtractor.csproj `
            /p:Configuration=Release `
            /p:Platform=x64 `
            /p:OutputPath="../../bundle/RevitExtractor.bundle/Contents/"
        shell: pwsh

      # ── 5. Verify DLL was produced ─────────────────────────────────────────
      - name: List bundle contents
        run: Get-ChildItem -Recurse bundle/
        shell: pwsh

      # ── 6. Zip the .bundle folder ──────────────────────────────────────────
      - name: Prepare bundle contents
        run: |
          # Show full bundle structure before zip
          Write-Host "=== Bundle structure ==="
          Get-ChildItem -Recurse bundle/
          Write-Host "=== Contents of zip will include ==="
          Get-ChildItem -Recurse bundle/RevitExtractor.bundle/
        shell: pwsh

      - name: Create AppBundle zip
        run: |
          # The .addin file must be at the ROOT of the .bundle folder
          # NOT inside Contents/ - Revit DA searches the package root
          $bundlePath = "bundle/RevitExtractor.bundle"
          $addinSrc   = "$bundlePath/RevitExtractor.addin"
          
          # Verify .addin exists at bundle root
          if (Test-Path $addinSrc) {
            Write-Host "✅ .addin found at bundle root: $addinSrc"
          } else {
            Write-Host "❌ .addin NOT found at: $addinSrc"
            exit 1
          }

          # Verify DLL was compiled into Contents/
          $dllPath = "$bundlePath/Contents/RevitExtractor.dll"
          if (Test-Path $dllPath) {
            Write-Host "✅ DLL found: $dllPath"
          } else {
            Write-Host "❌ DLL NOT found at: $dllPath"
            exit 1
          }

          Compress-Archive `
            -Path $bundlePath `
            -DestinationPath RevitExtractor.zip `
            -Force

          Write-Host "✅ Zip created"
        shell: pwsh

      # ── 7. Upload the zip as a workflow artifact (useful for debugging) ────
      - name: Upload zip artifact
        uses: actions/upload-artifact@v4
        with:
          name: RevitExtractor-bundle
          path: RevitExtractor.zip

      # ── 8. Publish AppBundle to APS ────────────────────────────────────────
      - name: Publish AppBundle to APS
        env:
          APS_CLIENT_ID:     ${{ secrets.APS_CLIENT_ID }}
          APS_CLIENT_SECRET: ${{ secrets.APS_CLIENT_SECRET }}
          APS_NICKNAME:      ${{ secrets.APS_NICKNAME }}
        run: |
          node scripts/publish-appbundle.js
        shell: bash

      # ── 9. Publish Activity definition ─────────────────────────────────────
      - name: Publish Activity to APS
        env:
          APS_CLIENT_ID:     ${{ secrets.APS_CLIENT_ID }}
          APS_CLIENT_SECRET: ${{ secrets.APS_CLIENT_SECRET }}
          APS_NICKNAME:      ${{ secrets.APS_NICKNAME }}
        run: |
          node scripts/publish-activity.js
        shell: bash

      # ── 10. Summary ─────────────────────────────────────────────────────────
      - name: Summary
        run: |
          echo "✅ AppBundle '${{ env.BUNDLE_NAME }}+${{ env.ALIAS }}' published."
          echo "✅ Activity 'ExtractRevitData+${{ env.ALIAS }}' published."
          echo "   Engine  : Autodesk.Revit+${{ env.ENGINE_VERSION }}"
          echo "   Outputs : result.json + result.csv"
        shell: bash
